[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "benny-care-system"
version = "0.1.0"
description = "Benny Care System"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]